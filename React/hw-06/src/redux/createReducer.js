import { ADD_CONTACTS, DELETE_CONTACTS, FIND_CONTACTS } from "./types";

const initialState = {
//   contacts: [],
  filter: ""
};
const addReducer = (state = initialState, action) => {
  switch (action.type) {
    case ADD_CONTACTS:
      return;
  }
};
